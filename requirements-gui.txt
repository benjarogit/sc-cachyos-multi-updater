# Python dependencies for Qt GUI version
# Install with: pip install -r requirements-gui.txt

# Qt6 Python bindings (choose one)
PyQt6>=6.6.0
# Alternative: PySide6>=6.6.0

# Optional: For Font Awesome icons (if not available, falls back to emoji)
# qtawesome>=1.2.0

